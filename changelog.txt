v4.7.5

- Re-handle invalid case animation keyframe. Succeed gracefully rather than failing
- Add `query.controller_speed` Molang Query.
- Remove the forRemoval deprecations in GeoModel for `getTextureResource` and `getModelResource`